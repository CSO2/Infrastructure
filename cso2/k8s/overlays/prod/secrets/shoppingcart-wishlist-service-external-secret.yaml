apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: shoppingcart-wishlist-service-secrets
  namespace: cso2-prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-prod
    kind: SecretStore
  target:
    name: shoppingcart-wishlist-service-secrets
    creationPolicy: Owner
  data:
  - secretKey: MONGODB_URI
    remoteRef:
      key: secret/data/cso2/prod/shoppingcart-wishlist-service
      property: MONGODB_URI
  - secretKey: REDIS_PASSWORD
    remoteRef:
      key: secret/data/cso2/prod/shoppingcart-wishlist-service
      property: REDIS_PASSWORD
